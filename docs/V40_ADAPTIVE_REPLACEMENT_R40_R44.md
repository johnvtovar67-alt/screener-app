# R40-R44 frozen adaptive replacement research

Frozen 2026-09-04 before execution. This inspected-development batch tests whether the strong R38 two-leader result can earn the unchanged 15-trade minimum through modestly faster scheduled rank replacement. It does not alter production authority.

The five bounded variants change only rank-retention buffer, broad-market review cadence, target count, or initial loss budget. All use causal close ranking with next-open execution, 12 bps slippage, zero commission, continuous holdings across reporting folds, and cash remains cash.

A survivor must pass every existing deterministic gate. It then proceeds to a strict 1,000-seed family-wise placebo and prospective tracking; historical folds cannot authorize live capital.

The strict placebo freezes R41 unchanged. For each seed it evaluates random ranking under all five matched lifecycle variants and retains the highest minimum return difference versus SPY and QQQ. R41 must exceed the 95th percentile of those 1,000 family maxima with an empirical family-wise p-value below 5%.
