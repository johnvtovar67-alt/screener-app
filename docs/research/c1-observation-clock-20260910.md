# C1 paper baseline observation clock

Read-only provenance audit: GitHub Actions run 34538492221. The saved production baseline was created 2026-09-10T15:59:42.924Z, but assigned September 10 as its first decision session. That day's opening auction preceded observation. Its baseline input is September 9. The archived record has no prospective sessions and must not be silently reset.

The corrected initialization selects the later of the next session after the baseline and the next opening after actual observation. Existing records with an inconsistent creation clock are rejected without mutation. Pending execution metadata uses the same lower bound. Tests cover immediately before open, exactly at open, and the actual production capture timestamp. Historical simulator and options are unchanged.

The same read-only audit found the September 9 source universe count changed from 224 to 226 and eligible count from 192 to 196. The new compiler identifies itself as a provisional current-cohort diagnostic, with pointInTime, survivorshipBiasFree and universeMembershipPointInTime all false. Refreshed research compilations are not an immutable prospective feed. Revised input records remain rejected. This audit does not establish the provider-level reason for price revisions.

No live authority, validation gate, or original historical record is changed. This corrects a paper-evidence timing defect, not evidence that any specific prior brokerage purchase was caused by this defect.
