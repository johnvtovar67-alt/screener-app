const fs=require('fs');const vm=require('vm');const assert=(c,m)=>{if(!c)throw new Error(m)};
let src=fs.readFileSync('lib/personalCapitalPolicy.js','utf8').replace(/export function /g,'function ');src+='\nmodule.exports={applyPersonalCapitalPolicy};';const sandbox={module:{exports:{}},exports:{},String,Object};vm.createContext(sandbox);vm.runInContext(src,sandbox);const{applyPersonalCapitalPolicy}=sandbox.module.exports;
const schw=applyPersonalCapitalPolicy({symbol:'SCHW',finalDecision:{action:'Strong Buy',reason:'qualified'}});assert(schw.finalDecision.action==='Avoid','SCHW must never remain actionable');assert(schw.finalDecision.personalCapitalBlocked===true&&schw.finalDecision.analyticalAction==='Strong Buy','SCHW block must preserve and disclose the analytical signal');assert(/\$5 million/.test(schw.finalDecision.reason)&&/no screener signal/i.test(schw.finalDecision.reason),'SCHW block must explain the personal concentration policy');
const other={symbol:'MS',finalDecision:{action:'Buy'}},unchanged=applyPersonalCapitalPolicy(other);assert(unchanged===other,'personal policy must not alter unrelated stocks');
console.log('PERSONAL CAPITAL POLICY PASS: SCHW is hard-blocked without changing unrelated signals.');
