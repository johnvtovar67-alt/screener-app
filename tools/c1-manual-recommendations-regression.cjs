const assert=require('node:assert/strict');
const {createResearchModuleLoader}=require('./research-module-loader.cjs');
const {buildC1ManualRecommendations:build,c1ManualReviewCurrent:current}=createResearchModuleLoader(process.cwd()).load('lib/c1ManualRecommendations.js');
const now=new Date('2026-09-14T14:00:00Z');
const decision={current:true,sourceSessionDate:'2026-09-11',decisionId:'fixture:7',revision:7};
const openingPlan={contract:'c1-actual-account-opening-plan-v1',providerVerified:true,sourceSessionDate:decision.sourceSessionDate,date:'2026-09-14',observedAt:now.toISOString(),sourceReceipt:{observedAt:now.toISOString(),membershipCheckedTwice:true,previousAdjustedClosesUnchanged:true},orders:[{id:'fixture:1',symbol:'AAA',side:'buy',shares:1,estimatedPrice:100,executable:false}]};
const args={decision,openingPlan,now};const before=JSON.stringify(args);
const review=build(args);assert.equal(review.status,'ready');assert.equal(current(review,decision,now),true);assert.equal(review.brokerageExecutionAuthorized,false);assert.equal(review.independentlyValidated,false);assert.equal(JSON.stringify(args),before);
for(const patch of [{pendingSession:{}},{openingError:'missing quote'},{decision:{...decision,current:false}},{now:new Date('2026-09-14T14:02:00Z')},{now:new Date('2026-09-14T21:00:00Z')},{openingPlan:{...openingPlan,providerVerified:false}},{openingPlan:{...openingPlan,sourceSessionDate:'2026-09-10'}},{openingPlan:{...openingPlan,sourceReceipt:{...openingPlan.sourceReceipt,previousAdjustedClosesUnchanged:false}}},{openingPlan:{...openingPlan,orders:[{...openingPlan.orders[0],symbol:'SCHW'}]}},{openingPlan:{...openingPlan,orders:[{...openingPlan.orders[0],symbol:'MSTR'}]}},{openingPlan:{...openingPlan,orders:[...openingPlan.orders,...openingPlan.orders]}}])assert.equal(build({...args,...patch}).status,'waiting');
assert.equal(current(review,{...decision,revision:8},now),false);assert.equal(current(review,{...decision,current:false},now),false);assert.equal(current(review,decision,new Date('2026-09-14T14:02:00Z')),false);
const fs=require('node:fs');const page=fs.readFileSync('pages/index.js','utf8');assert(page.includes('["opportunities","portfolio"].includes(tab)&&accountView&&<C1AccountOpportunities'));assert(page.includes('c1AccountMatchesPortfolio(accountView.decision,portfolio)?accountView.openingPlan:null'));assert(page.includes('alpha is not certified.'));
const api=fs.readFileSync('pages/api/c1-account.js','utf8');assert(api.includes('buildC1ManualRecommendations({decision,pendingSession,openingPlan,openingError,now:clock()})'));
console.log('PASS: manual authorization retains source/revision, market hours, freshness, provider verification, restricted purchases, and both-page account matching; no automatic brokerage authority or alpha certification.');
